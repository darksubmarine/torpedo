// Package memory is an output adapter to store entities in memory
package memory

import (
    "{{ .Meta.Package }}{{ .Meta.EntityPath }}/{{.PackageName}}"
)

type DMO struct {
    *{{.PackageName}}.EntityDMO // Do not remove it. This will let you add custom encrypted fields and more.

	/* your custom fields goes here */
}
